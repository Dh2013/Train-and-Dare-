/** Données soumises par le formulaire de contact */
export interface ContactFormValues {
  name: string;
  email: string;
  message: string;
}

/** Données soumises par le formulaire newsletter (footer) */
export interface NewsletterFormValues {
  email: string;
}
