# Local development. Never store backend secrets in VITE_* variables.
VITE_API_URL=http://localhost:3001/api

# Production: set the real HTTPS origin, or use Netlify's primary URL automatically.
VITE_SITE_URL=

# Absolute public API URL, including /api. Required by the Netlify release checks.
SEO_API_URL=
